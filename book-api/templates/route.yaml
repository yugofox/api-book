apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ include "book-api.fullname" . }}
  labels:
    {{- include "book-api.labels" . | nindent 4 }}
spec:
  to:
    kind: Service
    name: {{ include "book-api.fullname" . }}
  port:
    targetPort: http
