apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "book-api.fullname" . }}-test-connection"
  labels:
    {{- include "book-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "book-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
